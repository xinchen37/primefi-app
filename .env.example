# Public client identifier. Configure allowed origins in Reown Dashboard.
VITE_REOWN_PROJECT_ID=your_reown_project_id
# Set dev in .env.dev and prod in .env.prod.
VITE_APP_ENV=dev
# RPC and Multicall3 come from viem/chains. Generic RPC overrides are not used.
